<Window x:Class="Hackathon.YeniSifre"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        mc:Ignorable="d"
        Title="Yeni Şifre Belirle" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Icon="/Images/logo_icon.png">

    <Border CornerRadius="10" MouseDown="Border_MouseDown">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e3c72" Offset="0"/>
                <GradientStop Color="#1c1c2b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <!-- Pencere Kontrol Butonları -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0">
                <Button x:Name="btnMinimize" Click="Minimize_Click" Style="{StaticResource TitleBarButtonStyle}">
                    <Path Data="M0,8 H10" Stroke="White" StrokeThickness="1.5"/>
                </Button>
                <Button x:Name="btnClose" Click="Close_Click" Style="{StaticResource TitleBarCloseButtonStyle}">
                    <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="White" StrokeThickness="1.5"/>
                </Button>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
                <!-- Başlık -->
                <TextBlock Text="Yeni Şifre Oluştur" 
                           Foreground="White" FontSize="28" FontWeight="Bold" 
                           TextAlignment="Center" Margin="0,0,0,10"/>

                <!-- Açıklama Metni -->
                <TextBlock Text="Lütfen hesabınız için yeni ve güvenli bir şifre belirleyin." 
                           Foreground="#A0FFFFFF" FontSize="14" TextWrapping="Wrap" 
                           TextAlignment="Center" Margin="0,0,0,25"/>

                <!-- Giriş Alanları -->
                <Border BorderBrush="#20FFFFFF" BorderThickness="1" CornerRadius="8" Background="#0AFFFFFF" Padding="20">
                    <StackPanel>
                        <!-- Yeni Şifre -->
                        <TextBlock Text="Yeni Şifre" Foreground="#AAFFFFFF" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="pbSifreYeni" />

                        <!-- Yeni Şifre (Tekrar) -->
                        <TextBlock Text="Yeni Şifre (Tekrar)" Foreground="#AAFFFFFF" FontWeight="SemiBold" Margin="0,15,0,5"/>
                        <PasswordBox x:Name="pbSifreYeniTekrar" />

                       
                        <TextBlock x:Name="txtSifreHataMesaji"
                                   Foreground="#FFDA4242"
                                   TextWrapping="Wrap"
                                   Margin="0,10,0,0"
                                   Visibility="Collapsed"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <!-- Güncelle Butonu -->
                <Button x:Name="btnGuncelle"
                        Content="Şifreyi Güncelle" 
                        Click="Guncelle_Click" 
                        Style="{StaticResource mainButton}"
                        Width="250" Height="55"
                        Margin="0,30,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>