<Window x:Class="Hackathon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        mc:Ignorable="d"
        Title="Anasayfa" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Loaded="MainWindow_OnLoaded"
        Icon="/Images/logo_icon.png">

    <Window.Resources>
        <!-- Ders Butonları için Stil (Değişiklik yok) -->
        <Style x:Key="DersButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect x:Name="shadow" ShadowDepth="0" Color="#4CFFFFFF" Opacity="0" BlurRadius="20"/>
                            </Border.Effect>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                <Image Source="{Binding IkonYolu}" Width="60" Height="60" Margin="5"/>
                                <TextBlock Text="{Binding DersAdi}" FontSize="16" TextWrapping="Wrap" TextAlignment="Center" Margin="5,5,5,0"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#80FFFFFF"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="shadow" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="shadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#10FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Ayarlar ve Geri Butonu için Ortak Stil -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>
    
    

    <!-- Ana Konteyner -->
    <Border CornerRadius="10" MouseDown="TitleBar_MouseDown">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e3c72" Offset="0"/>
                <GradientStop Color="#1c1c2b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <!-- ▼▼▼ YENİ EKLENEN GERİ BUTONU ▼▼▼ -->
            <Button x:Name="btnGeri" 
                    Click="GeriButton_Click"
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Top" 
                    Margin="50,40,0,0" 
                    Width="50" Height="50"
                    ToolTip="Giriş Ekranına Dön"
                    Cursor="Hand"
                    Style="{StaticResource IconButtonStyle}">
                <Image Source="/Images/left_icon.png" Margin="8"/>
            </Button>

            <!-- ÜST BAŞLIK ALANI -->
            <StackPanel Margin="110,40,110,0" VerticalAlignment="Top" HorizontalAlignment="Center">
                <TextBlock x:Name="txtHosgeldin" Text="Hoş Geldin!" Foreground="White" FontSize="32" FontWeight="Bold" TextAlignment="Center"/>
                <TextBlock Text="Bugün hangi konuda kendini geliştirmek istersin?" Foreground="#A0FFFFFF" FontSize="18" Margin="0,5,0,0" TextAlignment="Center"/>
            </StackPanel>

            <!-- Sağ Üst Köşe Menü Butonları Grubu -->
            <StackPanel HorizontalAlignment="Right" 
            VerticalAlignment="Top" 
            Margin="0,40,50,0" 
            Orientation="Horizontal">

                <!-- ADMİN PANELİ BUTONU -->
                <Button x:Name="btnAdminPanel"
        Style="{StaticResource IconButtonStyle}"
                    Tag="Admin"
                        Cursor="Hand"
        Click="AdminPanelButton_Click"
        Width="50" Height="50">
                  

                    <Image Source="/Images/admin_logo.png" Margin="8"/>

                    <Button.ToolTip>
                        <ToolTip>
                            Admin Paneli: Uygulama ayarlarını ve kullanıcı verilerini yönetin.
                        </ToolTip>
                    </Button.ToolTip>
                </Button>

                <!-- AYARLAR BUTONU -->
                <Button x:Name="btnAyarlar" 
            Click="AyarlarButton_Click"
            ToolTip="Ayarlar ve Profil"
            Cursor="Hand"
            Style="{StaticResource IconButtonStyle}"
            Width="50" Height="50"
            Margin="10,0,0,0">
                  
                    <Image Source="/Images/Profil.png" Margin="8"/>
                </Button>

            </StackPanel>



            <!-- DERSLERİN GÖSTERİLECEĞİ OTOMATİK IZGARA -->
            <ItemsControl x:Name="DerslerItemsControl" Margin="50,150,50,50">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource DersButtonStyle}" 
                                Tag="{Binding DersAdi}" 
                                Click="Button_Soru"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            
                <Button x:Name="btnMinimize" Click="Minimize_Click" Style="{StaticResource TitleBarButtonStyle}">
                    <Path Data="M0,8 H10" Stroke="White" StrokeThickness="1.5"/>
                </Button>
               
                <Button x:Name="btnMaximize" Click="Maximize_Click" Style="{StaticResource TitleBarButtonStyle}">
                    <Path Data="M0,0 H10 V10 H0 Z" Stroke="White" StrokeThickness="1.2"/>
                </Button>
              
                <Button x:Name="btnClose" Click="Close_Click" Style="{StaticResource TitleBarCloseButtonStyle}">
                    <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="White" StrokeThickness="1.5"/>
                </Button>
            </StackPanel>

        </Grid>
    </Border>
</Window>