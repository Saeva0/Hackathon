<Window x:Class="Hackathon.BilgiEkrani"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        mc:Ignorable="d"
        Title="Bilgilendirme" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="TitleBar_MouseDown"
        Icon="/Images/logo_icon.png">
    
    
    <Border CornerRadius="10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e3c72" Offset="0"/>
                <GradientStop Color="#1c1c2b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık -->
            <TextBlock x:Name="txtBaslik" 
                       Grid.Row="0"
                       Text="Başlık Yükleniyor..." 
                       Foreground="White" 
                       FontSize="28" FontWeight="Bold" 
                       TextAlignment="Center"
                       Margin="0,40,0,20"/>

            <!-- Bilgilendirme Metni  -->
            <TextBlock x:Name="txtIcerik" 
                       Grid.Row="1" 
                       Foreground="#A0FFFFFF" 
                       FontSize="16" 
                       Margin="50,10"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       LineHeight="24">
                İçerik yükleniyor...
            </TextBlock>

            <!-- Onay Butonu  -->
            <Button x:Name="btnOnay" 
                    Grid.Row="2"
                    Content="Tamam"
                    Click="AnladimButton_Click"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,40"
                    Width="220" Height="45"
                    Cursor="Hand"
                    Foreground="White"
                    FontWeight="SemiBold"
                    FontSize="16">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FF4CAF50"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="22.5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF66BB6A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>