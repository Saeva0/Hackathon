<Window x:Class="Hackathon.Analiz"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        mc:Ignorable="d"
        Title="Performans Analizi" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Icon="/Images/logo_icon.png">

    <Border CornerRadius="10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e3c72" Offset="0"/>
                <GradientStop Color="#1c1c2b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- BAŞLIK ALANI -->
            <StackPanel Grid.Row="0" Margin="40,30">
                <TextBlock Text="Detaylı Performans Analizi" FontSize="32" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="txtAnalizKonu" Text="Konu: Yükleniyor..." Foreground="#A0FFFFFF" FontSize="18" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- İÇERİK ALANI -->
            <Grid Grid.Row="1" Margin="40,0,40,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <!-- Sol Taraf: Grafikler ve Özet -->
                    <ColumnDefinition Width="3*"/>
                    <!-- Sağ Taraf: Yapay Zeka Tavsiyeleri -->
                </Grid.ColumnDefinitions>

                <!-- SOL TARAF -->
                <StackPanel Grid.Column="0">
                    <Border Background="#1AFFFFFF" CornerRadius="8" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Genel Performans" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="DOĞRU" Foreground="#80FFFFFF" FontWeight="Bold"/>
                                    <TextBlock x:Name="txtAnalizDogru" Text="-" FontSize="48" Foreground="#77FF77" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="YANLIŞ" Foreground="#80FFFFFF" FontWeight="Bold"/>
                                    <TextBlock x:Name="txtAnalizYanlis" Text="-" FontSize="48" Foreground="#FF7777" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="BAŞARI" Foreground="#80FFFFFF" FontWeight="Bold"/>
                                    <TextBlock x:Name="txtAnalizBasari" Text="-%" FontSize="48" Foreground="#3498db" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Border Grid.Column="1" Background="#1AFFFFFF" CornerRadius="8" Padding="25" Margin="20,0,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Yapay Zeka Geri Bildirimi" FontSize="22" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>

                            <!-- 1. Tavsiye Metni: Başlangıçta kullanıcıyı bilgilendiren bir metin gösterecek -->
                            <TextBlock x:Name="txtAITavsiye" 
                       Text="Performansınız hakkında kişiselleştirilmiş yorum ve tavsiyeler almak için analizi başlatın."
                       Foreground="#A0FFFFFF" FontSize="16" TextWrapping="Wrap" LineHeight="26" FontStyle="Italic"/>

                            <!-- 2. Yeni Analiz Başlatma Butonu -->
                            <Button x:Name="btnAnalizBaslat" 
                    Content="Analizi Başlat" 
                    Click="AnalizBaslat_Click"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0"
                    Width="200" Height="45"
                    Background="#27ae60" 
                    Foreground="White" FontWeight="Bold">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="22.5">
                                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#2ecc71"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- ANA MENÜYE DÖN BUTONU -->
            <Button Grid.Row="2" Content="Ana Menüye Dön" HorizontalAlignment="Right"
                    Margin="0,0,40,20" Width="180" Click="AnaMenuButton_Click"/>
        </Grid>
    </Border>
</Window>