<Window x:Class="Hackathon.SifremiUnuttum"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        mc:Ignorable="d"
        Title="Şifremi Unuttum" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Icon="/Images/logo_icon.png">

    <Border CornerRadius="10" MouseDown="Border_MouseDown">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e3c72" Offset="0"/>
                <GradientStop Color="#1c1c2b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <!-- Pencere Kontrol Butonları -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0">
                <Button x:Name="btnMinimize" Click="Minimize_Click" Style="{StaticResource TitleBarButtonStyle}">
                    <Path Data="M0,8 H10" Stroke="White" StrokeThickness="1.5"/>
                </Button>
                <Button x:Name="btnClose" Click="Close_Click" Style="{StaticResource TitleBarCloseButtonStyle}">
                    <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="White" StrokeThickness="1.5"/>
                </Button>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
                <!-- Başlık -->
                <TextBlock Text="Şifreni Sıfırla" 
                           Foreground="White" FontSize="28" FontWeight="Bold" 
                           TextAlignment="Center" Margin="0,0,0,10"/>

                <!-- Açıklama Metni -->
                <TextBlock Text="Hesabına bağlı e-posta adresini girerek şifre sıfırlama talimatlarını alabilirsin." 
                           Foreground="#A0FFFFFF" FontSize="14" TextWrapping="Wrap" 
                           TextAlignment="Center" Margin="0,0,0,25"/>

                <!-- E-posta Giriş Alanı -->
                <Border BorderBrush="#20FFFFFF" BorderThickness="1" CornerRadius="8" Background="#0AFFFFFF" Padding="20">
                    <StackPanel>
                        <TextBlock Text="E-posta Adresi" Foreground="#AAFFFFFF" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtEmail" Style="{StaticResource textBox}" 
                                 Padding="8" FontSize="14"
                                 Background="#E6F0F8" Foreground="#1e3c72"/>
                    </StackPanel>
                </Border>

                <!-- Kod Gönder Butonu -->
                <Button x:Name="btnKodGonder" 
                        Content="Doğrulama Kodu Gönder" 
                        Click="KodGonder_Click" 
                        Style="{StaticResource mainButton}"
                        Width="250" Height="55"
                        Margin="0,30,0,0"/>
            </StackPanel>

            <!-- Geri Dön Butonu -->
            <Button Content="← Giriş Ekranına Dön" 
                    Click="Geri_Click"
                    Background="Transparent" BorderThickness="0"
                    Foreground="#80FFFFFF" Cursor="Hand"
                    HorizontalAlignment="Center" VerticalAlignment="Bottom"
                    Margin="0,0,0,20">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>