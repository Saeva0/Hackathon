<Window x:Class="Hackathon.KodDogrulama"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackathon"
        mc:Ignorable="d"
        Title="Kodu Doğrula" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Icon="/Images/logo_icon.png">

    <Border CornerRadius="10" MouseDown="Border_MouseDown">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e3c72" Offset="0"/>
                <GradientStop Color="#1c1c2b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <!-- Pencere Kontrol Butonları -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0">
                <Button x:Name="btnMinimize" Click="Minimize_Click" Style="{StaticResource TitleBarButtonStyle}">
                    <Path Data="M0,8 H10" Stroke="White" StrokeThickness="1.5"/>
                </Button>
                <Button x:Name="btnClose" Click="Close_Click" Style="{StaticResource TitleBarCloseButtonStyle}">
                    <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="White" StrokeThickness="1.5"/>
                </Button>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="450">
                <!-- Başlık -->
                <TextBlock Text="Doğrulama" 
                           Foreground="White" FontSize="28" FontWeight="Bold" 
                           TextAlignment="Center" Margin="0,0,0,10"/>

                <!-- Açıklama Metni -->
                <TextBlock x:Name="txtBilgi" 
                           Text="E-posta adresinize gönderilen 6 haneli kodu girin." 
                           Foreground="#A0FFFFFF" FontSize="14" TextWrapping="Wrap" 
                           TextAlignment="Center" Margin="0,0,0,25"/>

                <!-- Kod Giriş Alanı -->
                <Border BorderBrush="#20FFFFFF" BorderThickness="1" CornerRadius="8" Background="#0AFFFFFF" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Doğrulama Kodu" Foreground="#AAFFFFFF" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <StackPanel x:Name="CodeEntryPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBox x:Name="txt1" Style="{StaticResource CodeBoxStyle}" PreviewTextInput="CodeBox_PreviewTextInput" TextChanged="CodeBox_TextChanged" PreviewKeyDown="CodeBox_PreviewKeyDown"/>
                            <TextBox x:Name="txt2" Style="{StaticResource CodeBoxStyle}" PreviewTextInput="CodeBox_PreviewTextInput" TextChanged="CodeBox_TextChanged" PreviewKeyDown="CodeBox_PreviewKeyDown"/>
                            <TextBox x:Name="txt3" Style="{StaticResource CodeBoxStyle}" PreviewTextInput="CodeBox_PreviewTextInput" TextChanged="CodeBox_TextChanged" PreviewKeyDown="CodeBox_PreviewKeyDown"/>
                            <TextBox x:Name="txt4" Style="{StaticResource CodeBoxStyle}" PreviewTextInput="CodeBox_PreviewTextInput" TextChanged="CodeBox_TextChanged" PreviewKeyDown="CodeBox_PreviewKeyDown"/>
                            <TextBox x:Name="txt5" Style="{StaticResource CodeBoxStyle}" PreviewTextInput="CodeBox_PreviewTextInput" TextChanged="CodeBox_TextChanged" PreviewKeyDown="CodeBox_PreviewKeyDown"/>
                            <TextBox x:Name="txt6" Style="{StaticResource CodeBoxStyle}" PreviewTextInput="CodeBox_PreviewTextInput" TextChanged="CodeBox_TextChanged" PreviewKeyDown="CodeBox_PreviewKeyDown"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid Margin="0,30,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Soldaki "Kodu Doğrula" Butonu -->
                    <Button x:Name="btnDogrula" 
            Grid.Column="0"
            Content="Kodu Doğrula" 
            Click="Dogrula_Click" 
            Style="{StaticResource mainButton}"
            Width="200" Height="55"
            HorizontalAlignment="Right"
            Margin="0,0,10,0"/>

                    <!-- Sağdaki "Tekrar Gönder" Butonu ve Sayaç -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,0">

                        <Button x:Name="btnTekrarGonder" 
                        Content="Tekrar Kod Gönder" 
                        Click="TekrarGonder_Click" 
                        Width="200" Height="55"
                        IsEnabled="False">          

                           
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource mainButton}">
                                 
                                    <Setter Property="Background" Value="#555555"/>
                                    <Setter Property="BorderBrush" Value="#777777"/>
                                    <Setter Property="Foreground" Value="#AAAAAA"/>

                                    <!-- Stilin Tetikleyicileri -->
                                    <Style.Triggers>
                                       
                                        <Trigger Property="IsEnabled" Value="True">
                    
                                            <Setter Property="Background" Value="#FF007ACC"/>
                                       
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>

                                      
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsEnabled" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="#FF3399DD"/>
                                          
                                        </MultiTrigger>

                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
        

                        </Button>



                       

                        <TextBlock x:Name="txtGeriSayim"
           Text="Yeni kod istemek için 60 saniye bekleyin."
           Foreground="#A0FFFFFF" FontSize="12"
           HorizontalAlignment="Center" Margin="0,5,0,0"
           Opacity="1"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

        </Grid>
    </Border>
</Window>